# Switzy for Windows

This directory is a placeholder for future Windows work.

There is no supported Windows app, CLI, installer, or release artifact yet. Shared Git and SSH logic currently lives in `../Shared/Sources/SwitzyCore`.
