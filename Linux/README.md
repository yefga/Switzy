# Switzy for Linux

This directory is a placeholder for future Linux work.

There is no supported Linux app, CLI, installer, or release artifact yet. Shared Git and SSH logic currently lives in `../Shared/Sources/SwitzyCore`.
